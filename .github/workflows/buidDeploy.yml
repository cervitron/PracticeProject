name: BuildDeploy
on: push
runs-on: ubuntu-latest
jobs:
  checkout:
    steps:
      - name: checkout
        uses: actions/checkout@v3 
  
  installDependencies:
    steps:
      - name: install
        run: npm ci
  
  build:
    steps:
      - name: build
        run: npm run build
  
  tests:
    steps:
      - name: tests
        run: npm test    
  
     
  deploy:
    needs: tests 
    runs-on: ubuntu-latest
    steps:
      - name: deploy
        run: echo "desplegando" 